# USACO Solutions

## Repository layout

- `cowdependence/`
	- `main.cpp` — C++ solution (primary, fast - passes all tcs).
	- `main.py`  — Python implementation (passes most tests; slower).
- `interstellar-intervals/`
	- `main.py` — Python solution. (passes all tcs)
- `job-completion/`
	- `main.py` — Python solution. (passes all tcs)